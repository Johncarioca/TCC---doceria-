import { con } from "./conection";





///////////////////// tebela produto ///////////////////////

export async function listaProduto(){

    const comando =
    `

    `
    const [resposta] = await con.query(comando);
}